# The Quiet Line source art

The two source folders are authoritative for the demo atlases:

- `quiet-line-tiles/`
- `quiet-line-sprites/`

Rebuild them with `tools/pack_atlas.py`. The committed atlas PNGs and generated snippets were produced from these folders without padding or resizing.
